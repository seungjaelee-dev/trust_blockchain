# 첫 세션 프롬프트

아래 구분선 이후를 프로젝트 루트의 Codex CLI에 붙여 넣으세요.

---

AGENTS.md, README.md, docs/PLAN.md, docs/PROGRESS.md,
reference/TRACK1_README.md와 reference/schema.json을 읽어라.
프로젝트는 TRUST404 ① 정적 분석기로 전환됐다. 이전 로그·영수증 프로젝트는 구현하지 마라.

오늘은 M0만 진행한다. 나는 Python/C 경험이 있지만 Solidity/정적 분석을 배우는 단계다.
긴 문법 수업 대신 P2와 P4의 '입력 → 호출 권한 → 검사 → 상태 변경'을 실제 코드로 설명해라.

1. 기존 파일을 보존하면서 현재 경로·Git 상태·WSL/Python/solc/Slither 상태를 확인한다.
2. 필요한 최소 환경을 준비한다. Slither 공식 코드/API 예제를 읽고 설치 버전과 API를 확인한다.
   지금은 개발 환경 설정 단계이며, 채점 실행이 인터넷 설치에 의존하지 않도록 패키징 과제를 남긴다.
3. reference/public/P2_HiddenMint.sol과 P4_CappedMint.sol을 solc 0.8.20 및 Slither로 읽는다.
4. 실제 API로 함수 목록·인자·modifier 조건·상태변수 쓰기·조건 노드·원본 위치를 추출한다.
   debug 구조 출력과 채점 출력은 분리한다. AST를 '그냥 JSON'이라 부르더라도 실제 API 타입을 확인한다.
5. 최소 Python 패키지와 폴더 입력 CLI를 만든다. 아직 판정 규칙이 없는 파일은 사실에 맞는 이유와
   UNCERTAIN을 내며, schema를 통과하는 JSON 배열 하나만 stdout에 출력한다.
6. 정상 파일과 실패 파일이 섞여도 각 결과를 내는지, 로그가 stderr에 있는지 확인한다.
7. 파일명과 LABEL 주석에 의존하지 않았음을 확인한다. 라벨 파일은 평가에만 사용한다.
8. README/PROGRESS에 실제 명령·버전·성공/실패·패키징 미완료 상태를 남긴다.

M0의 전부 UNCERTAIN은 개발 중간 상태다. 채점 점수를 얻는 완성본이라고 주장하지 마라.
오늘 웹·추가 ML·컨트랙트 배포는 만들지 않는다.
설치/일상 수정은 현재 권한 안에서 자율적으로 진행하고, 막힌 작업은 원인과 안전한 대안을 제시한다.

완료 후 내가 직접 실행할 명령과 읽을 핵심 함수 2개를 알려줘.
공개 원본을 보존한 별도 사본에서 P4 상한 검사를 제거하고, 구조 출력이 어떻게 달라지는지 실습하게 해줘.
'onlyOwner면 왜 무조건 안전하지 않은가?', 'MAX_SUPPLY가 선언된 것과 강제되는 것은 어떻게 다른가?'를
코드 흐름으로 설명해줘. 내가 아직 실습하지 않았다면 이해 완료로 기록하지 마라.
