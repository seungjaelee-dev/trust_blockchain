# 진행 기록

## 현재 상태 — 2026-09-19

- 기본 파일별60초/전체540초, 디렉터리 JSON CLI·오류/timeout 격리·자식 프로세스 종료.
- 독립 판정 규칙7개와 제한된 경로 해석, 장부/금고 지원 확대, 함수별 흐름 보고 구현.
- 문서·고지를 포함한 현재 이미지의 전체31 테스트 통과(198.769초), 오프라인2CPU/4GB 혼합60개 기대 판정·근거 연결 검증 통과(38.276초). [제출 검증](experiments/submission-check.json) 참조.
- 현재 실행/지원 범위는 README, 제출 전달 절차는 SUBMISSION, 검증 데이터는 experiments/ 참조.
- 제출용 문서·개발 지침·제3자 고지 정리. 최종 commit/push 및 지정 방식의 이미지 전달은 별도 작업.
- 목표 제출일9/20. 공식 마감 시각과 전달 형식은 주최 측 공통 안내를 따른다.

## 과거 기록 읽는 법

아래는 당시 상태를 보존한 이력이다. 예전30초 제한, 테스트 수, 미완료 메모는 현재 설정을 뜻하지 않는다.
[9/15~17 이력](history/2026-09-15_17.md)과 [실험 목록](experiments/README.md)을 함께 참고한다.

## 9/18 사용자 자원 제한 시험

- 사용자가 WSL 설정 및 CLI 실행을 직접 수행하고 터미널 결과를 대화에 제공.
- nproc=2, RAM=3.8Gi, Swap=0B. 개발40 분석 real=28.704초.
- 전달된 JSON은 정상24/위험16으로 기대 판정과 일치. results/limited.json·limited.log 보존.
- 이는 사용자 보고 및 제공된 결과에 근거한 기록. 깨끗한 오프라인 재현/비공개셋 성능/일반 부하 보장은 아님.
- 사용자의 CLI 실행은 확인했으나 모든 판정 규칙의 이해/변형 실습 완료는 아직 확인하지 않음.

## 남은 일

- 제출 OS/아키텍처·Docker 반입·용량/제출 경로 확인.
- 최종 이미지 전달 검증과 라이선스 고지 정리. Docker 의존성 고정·빌드·공개 표본 실행 완료.
- 최종 Docker 혼합60개 검증 완료. 임의 대형 입력·전체300개 재실행은 이번 범위에서 수행하지 않음.
- 중복 evidence와 파일별 구체적 근거 문구 개선, 제출 자료 작성.
- 웹·독립 비공개 평가 성능은 아직 없음. 다음 계획은 PLAN.md.

## 9/18 디렉터리·문서 정리

- 사용자 요청: 불필요한 문서/파일 정리, README와 현재 상태 갱신.
- README를 현재 WSL 실행법·검증·범위·구조·제출 미완료 항목 중심으로 재작성.
- PLAN의 지난 일정/미구현 가상 파일 목록을 제거하고 남은 제출 작업으로 정리.
- 초기 문서 FIRST_TASK.md, docs/R1.md, docs/WORKER_FLOW.md의 중복 안내는 EXPANDED_RULES.md로 통합 후 삭제.
- 일회성 API 관찰 도구 scripts/inspect_paths_api.py와 중간 cycle1/cycle2 결과·로그 삭제.
  재사용 가능한 inspect_sol.py/inspect_ir.py, 회귀 테스트, 원본 표본, 사용자 limited 결과는 보존.
- 실험 JSON4개를 docs/experiments/로 이동. 이전 snapshot은 9/17 당시 경로/해시를 나타내는 이력으로 유지.
- 긴 과거 PROGRESS는 history/에 보존. 당시 문서/도구 경로는 현재와 다를 수 있음.
- 판정 로직/CLI 계약/테스트 입력은 변경하지 않음. 실험 비교 스크립트의 저장 경로만 새 위치로 맞춤.
- 검증: 현재 문서의 로컬 링크 정상, 실험 JSON4개 읽기 정상, 남긴 스크립트4개 Python AST 구문 통과. 이동된 저장 폴더 확인 및 원본 reference Git diff 없음. limited.json 40개를 기대 정답과 기계적으로 재비교해 일치 확인. 분석 로직 변경이 없어 전체 회귀17개는 재실행하지 않음.
- 다음 사용자 실습: EXPANDED_RULES.md의 cap_branch 정상/위험 한 쌍을 비교하고 우회 경로를 설명.

## 9/18 구조 이해 HTML 추가

- 사용자 요청: AI로 작성된 전체 코드 흐름을 본인이 이해하고 심사위원에게 설명할 수 있도록, 기초부터 설명하는 HTML 문서 작성.
- `docs/project-flow-guide.html` 추가. 탭 구조로 문제 기초, 실행 흐름, 폴더 지도, 판정 규칙, 심사 답변, 말로 설명 연습을 정리.
- 실제 호출 흐름은 `scripts/analyze.py` → `src/track1/cli.py` → `src/track1/worker.py` → Slither/solc → `src/track1/verdict.py` → `src/track1/rules/*.py` → stdout JSON으로 설명.
- 사용자 추가 요청에 따라 Python import/subprocess 흐름, worker와 Slither 연결, `verdict.py`의 `RULES` 호출 구조, 복수 위험 사유의 JSON 집계 방식을 코드 예시와 함께 보강.
- 이어서 입력 폴더 인자 → `.sol` 파일 필터링 → 기본 `UNCERTAIN` 결과 리스트 → for문 분석 → worker subprocess → Slither 분석 객체 → rules 호출 → JSON 배열 출력의 전체 로직을 8단계 코드 흐름으로 추가.
- `src/track1/rules/`의 각 파일이 직접 판정 규칙인지 보조 해석기인지, 어떤 위험/오류를 잡기 위한 파일인지, 핵심 함수가 무엇인지 표로 추가. Slither/solc가 제공하는 구조 분석과 프로젝트 자체 판정 로직의 경계도 설명.
- README의 파일 안내에 HTML 설명서 링크 추가.
- 문서는 이해 보조 자료이며, 사용자가 내용을 직접 설명해 본 상태는 아직 확인하지 않음.

## 9/18 협업 후보 저장소 코드 비교

- 사용자 요청으로 yunu471/trust404-track01의 analyzer.py, README, run.sh, 검증 스크립트를 읽고 현재 CLI/worker/verdict/규칙 구조와 비교.
- 비교 대상 커밋: 2792b80a75dcb0b23f2c750575047322c4454730. 외부 코드를 실행하거나 분석기에 합치지는 않음. 성능 수치의 비교 실험은 수행하지 않음.
- 상대 구현은 Python 표준 라이브러리와 정규식/괄호 매칭을 사용하여 설치가 간단함. 현재 구현은 Slither/solc의 구조 정보와 제한된 경로 해석을 사용하며 오프라인 패키징이 남아 있음.
- 코드 검토상 상대 구현의 이름 의존, 상한 검사와 실제 증가량 연결 부재, 탐지 없음/개별 탐지 예외 이후 BENIGN 가능성, delegatecall 예치 경로 연결 부재를 확인.
- typ_immutable_check가 발견한 상태변수 타입에 immutable 문자열을 붙이는 문제와 전체 시간 예산 부재도 확인. 실행 재현 전이므로 실제 오탐 수나 정확도로 보고하지 않음.
- 협업 제안: 현재 구조 분석 기반을 유지하고 서로 만든 변형 문제로 교차 평가한 뒤, 실패 원인을 검토하여 규칙을 개선. 단순히 두 분석기의 MALICIOUS 결과를 합치는 방식은 피함.
- 다음 단계 후보는 동일 입력/정답으로 두 구현의 판정·근거·시간을 비교하는 교차 실험. 사용자의 규칙 이해 완료는 아직 확인하지 않음.

## 9/18 Docker 환경 준비

- 사용자에게 Docker 실행 허용을 확인받아 Dockerfile, .dockerignore, docker/requirements.lock,
  scripts/prepare_docker.py, docs/DOCKER.md를 추가. 분석 로직은 변경하지 않음.
- 실제 WSL의 Python 3.12.3 및 설치 패키지 51개의 버전을 기록. pip check 정상.
  Python 버전은 현재 개발 버전이며 대회 필수 버전이라고 확인된 것은 아님.
- Linux amd64 solc 0.8.20의 SHA256을 확인하고 artifacts/docker로 복사.
  준비 스크립트와 이미지 빌드 양쪽에서 같은 SHA256을 검사하도록 구성.
- 복사한 solc를 명시한 WSL CLI 실행 종료 코드 0, 공개5 판정 BENIGN/MALICIOUS/MALICIOUS/BENIGN/MALICIOUS 확인.
- 개발 시 src/scripts 읽기 전용 연결, 제출 시 소스 포함 이미지, 네트워크 차단·2CPU/4GB 실행,
  docker save/load와 이미지 tar 해시 기록 명령을 문서화.
- 현재 Windows 표준 설치 경로와 WSL에 Docker 실행 파일/소켓이 없어 이미지 build/run은 미검증.
  사용자에게 Docker 설치 상태를 질문. 설치 후 빌드·공개/변형/실패 입력 검증을 이어가야 함.
- 기본 이미지는 Python 정확한 버전 태그이며 digest 고정 전. 의존성은 버전 lock이며 배포 파일 해시 lock은 아님.
  최종 이미지 및 라이선스 고지/소스 제공 정리, 오프라인 전달 검증은 미완료.
- 사용자 Docker 실행 실습/이해 완료는 아직 확인하지 않음.

## 9/19 Docker 재현과 제출용 README 정리

- 이전 Dockerfile 수정으로 HOME=/home/trust1과 UID/GID 10001의 쓰기 권한을 설정. bundled solc와 판정 로직은 유지.
- 이전 실제 검증: Docker build 성공, --network none·2CPU/4GB 공개5 판정 일치, PermissionError 없음.
- 사용자 보고: 다른 디렉터리에서 clone한 뒤 Docker build/run과 환경 확인 정상. 별도 컴퓨터 검증으로 확대 해석하지 않음.
- README를 clone → build → offline run 중심의 제출용 대표 문서로 재작성. 개인 가상환경 경로 제거.
- JSON 계약·시간 예산·판정 원리·지원 한계·환경 버전·검증 기록·기여·이미지 save/load 절차 정리.
- DOCKER의 미검증/solc Git 제외 안내를 현재 상태로 수정하고 PLAN도 갱신.
- 이번 작업은 문서만 변경. 분석 테스트 재실행 및 새 혼합 오류 입력 검증은 하지 않음. 사용자는 혼합 오류 검증을 다음 날 진행 예정.
- 사용자의 Docker 재현 실행은 확인했으나 전체 규칙 이해 완료는 기록하지 않음.
- 다음: 최종 Docker 혼합 오류 검증, 배포 고지·이미지 전달·제출 형식 확인.

## 9/19 합성 300개 ZIP 원본 검토 — 구현 보류

- 사용자 요청으로 ZIP의 정답표·패턴별 대표 소스와 추가 사례를 읽고 현재 규칙과 비교.
- 원본 300개 SHA256 전부 일치 확인. 분석기 재실행/300개 개별 수동 심사는 하지 않음.
- 총공급량 필수 조건, 직접 전송 필수 조건, 조건 없는 입금만 허용하는 제약 및 DIVISION 미지원 확인.
- U009 비대칭 정지 등 대회 기준과 합성 라벨 충돌 및 M016 등 피해 상태 도달 가능성 문제 발견.
- 개선 우선순위·검증·라벨 검토 원칙은 DATASET_300_REVIEW.md에 기록. 구현/런타임 변경 없음.
- 다음 단계: 원본 기준과 대회 기준 이견표를 구분하고, 사용자 구현 요청 후 장부/총공급량 분리와 입금 상한부터 작은 단위로 검증.
- 사용자 이해 실습 완료는 확인하지 않음.

## 9/19 장부 규칙 구현·실행 완료

- 사용자 승인에 따라 ledger_operations.py 추가 및 verdict RULES 등록. 총공급량이 없는 자기/승인 전송, 동일량 소각, 대칭/비대칭 정지, 관리자 지정 목적지 제한, 전송 없는 고정 상한 수량 발행을 지원.
- expanded_vaults.py에 자기 입금 상한과 사용자 장부 차감 없는 관리자 임의 ETH 송금 분석 추가.
- 피해 장부 도달 가능성, 초기 수량0, 양수 승인 생성 불가, 잘못된 승인 인덱스/차감액, 변하지 못하는 정지 상태, 별도의 대칭 제한 등 반례를 추가하고 보류/정상 경계를 보강.
- 기존17 회귀 통과. 신규6 포함 전체23 테스트 통과(272.955초). 마지막 승인·제약 보강 후 관련 테스트 각각 재실행 통과.
- 기존 homefix Docker 대표14개: 전부 UNCERTAIN. 새 규칙: BENIGN6/MALICIOUS5/UNCERTAIN3로 대회 기준 검토값과 일치.
- 최종 trust404-track1:ledger-review 빌드 후 --network none·2CPU/4GB·swap추가없음, 공개5+개발40+대표14+문법오류1 = 60개 실행. 83.398초, 기대 판정60/60·schema·근거 줄 범위·파일 누락없음·PermissionError없음 확인.
- 혼합 오류 격리도 실제 확인: 오류 파일만 UNCERTAIN, 종료코드0, stdout 단일 JSON 배열.
- docs/experiments/ledger-{snapshot,integration,baseline}.json에 이미지ID·소스/문제 해시·실제 출력 보존. 재현 스크립트 tests/evaluate_ledger_docker.py 추가.
- 원본 ZIP/정답표와 reference/schema, Docker 환경 버전은 변경하지 않음. 전체300 재실행/독립 평가/EVM 공격 실행은 하지 않음. 수수료·timestamp/struct/루프·proxy 확대는 후순위 유지.
- README/분석 원리/검토 계획/출처 문서 갱신. 사용자 실습·규칙 이해 완료는 아직 확인하지 않음.
- 사용자 실습: delegated.sol 사본에서 승인 차감문을 제거하고 승인 재사용 경로를 설명. 다음: 최종 제출 태그로 재빌드·전달 방식/라이선스 고지 확인. Git push는 하지 않음.

## 9/19 새 trust404_solidity_300 평가 사용법 안내

- Downloads/trust404_solidity_300의 README, metadata, 정답 일부와 evaluate.py를 읽고 cases 바로 아래 .sol 300개를 확인. 이전 solidity_contract_dataset_300과 별도 자료로 취급.
- WSL의 cases 입력 경로, 결과 JSON 저장, 동봉 evaluate.py로 정답표와 file/verdict를 비교하는 절차 안내.
- 평가기는 UNCERTAIN을 정답과 일치해도 보류로 집계하며, reasons/evidence의 타당성 및 전체 schema를 검증하지 않음을 확인.
- 분석기 및 외부 평가기는 실행하지 않음. 이번 300개 성능/실행 오류는 확인하지 않음.
- 사용자는 디렉터리 분석 명령과 stdout/stderr 리다이렉션 의미를 안다고 설명. 채점 지표 이해와 실습 완료는 아직 미확인.
- 다음 실습: 사용자가 분석 및 채점을 실행하고 correct/wrong/uncertain 합계와 coverage를 해석.

## 9/19 이유 타당성·근거 위치 검토 (코드 변경 없음)

- 사용자 질문에 따라 TRACK1_README.md 187~192행의 이유0/1/2점 기준과 실제 혼합60 JSON, 규칙 생성부·집계부·테스트를 대조.
- line은 Slither source_mapping에서 실제 노드 시작 줄을 얻는다. 기존 검증은 범위/Unicode/일부 특정 연산 위치를 확인했으며 모든 설명 주장과 근거의 일치를 검증한 것은 아니다.
- admin_exemption의 관리자 예외 설명은 핵심 조건17행/설정14행 대신 잔액 차감19행만 근거로 제시. destination_block도 조건 대신 차감 줄만 제시.
- approval_bypass는 관리자 분기20행/승인21~22행/이동24~25행을 연결하면 더 구체적이며 현재 설명은 권한 주체가 일반화되어 있음.
- bounded_issuance의 상한 안전 설명에 상한 검사16행 대신 증가17행만 표시. 조건·권한·상태 변경을 각각 근거로 수집할 필요.
- 실제60개 출력 중14개에서 evidence 중복, 중복 초과 항목23개 확인. verdict.py는 규칙 결과를 그대로 이어 붙임.
- UNCERTAIN은 규칙별 Unsupported 메시지를 이어 붙여 핵심 한계와 무관한 규칙 적용 실패가 혼재. 분석기 미지원과 코드 자체의 정보 부족을 구분해야 함.
- 정상 설명은 존재하지 않는 전송/소각/정지 패턴까지 공통 문구로 언급하는 경우가 있어 실제 발견 연산별 문장 생성 권장.
- 개선 우선순위: 주장별 노드 보존 → 입력별 사실 기반 이유 생성 → 핵심 보류 사유 선정/진단 분리 → 중복 제거 → 주장·근거 연결 회귀 테스트.
- 이번에는 검토만 수행하고 분석/출력 코드는 변경하지 않음. 심사점수 또는 모든 사례의 의미적 정확성을 보장하지 않음.

## 9/19 사용자 seung 결과 시간 제한 제외 평가

- 기존 results/seung.json/log/score와 Downloads/trust404_solidity_300의 원본 정답표를 비교. 분석기는 재실행하지 않음.
- CLI의 정확한 시간 제한 사유로만 제외: case_188 중단1개, case_189~300 미착수112개. 300개 파일명 유일성과 정답 대응 및 합계 검증.
- 제외 후187개: 정답27, 오답3, 보류157, coverage16.04%, 확정 정확도90%, 순점수24. 원본 정답 기준 유지.
- 보류157개는 정답 BENIGN61/MALICIOUS62/UNCERTAIN34. 24개 유형 중16개 유형에서 확정 판정 없음.
- 원본 기준 오답 case_028/068/156은 모두 송금 뒤 checked -= 차감 재진입 구조. Solidity 0.8 underflow revert와 원본 해설(반복 초과 인출 가능)의 충돌 소지 확인. 소스/공식 언어 문서 검토이며 EVM 공격 재현은 미실행; 정답 수정 없음.
- 결과: results/seung_score_without_timeout.json, 전체 파일별 비교 results/seung_timeout_comparison.csv. 기존 결과/정답/분석 코드 보존.
- 다음: 세 불일치 사례의 실행 기반 라벨 검토 및 미지원 보류 패턴 우선순위 검토. 사용자 이해/실습 완료는 아직 미확인.

## 9/19 판정 이유·위치 연결 구현

- 사용자 승인 범위: 위험도·위험유형·confidence를 추가하지 않고 기존 네 JSON 필드 안에서 설명 개선. 이번 단계의 판정 조건/우선순위는 유지.
- reporting.py에 주장별 근거 노드 보존·함수/줄 접두사·조건/상태 변경/외부 호출 설명·완전 중복 제거 구현. 위험 요약에 권한 검사·관리 상태 설정·실제 자산 변경 위치를 연결.
- 정상 결론은 컨트랙트 전체 범위로 표시하고 장부 규칙의 전송/소각/정지 문구를 실제 발견 연산별로 구성. 여러 분기는 성공 경로 번호로 구별.
- Unsupported에 선택적 노드를 연결해 미지원 연산/IR/경로 노드의 위치를 표시. 위치 미확보 오류는 분석 단계로 표시. 규칙별 상세 실패 로그는 stderr로 분리.
- reporting 회귀: 권한/상한 검사 줄, 이름 변경·Unicode와3줄 이동, 분기 극성, 미지원 나눗셈 위치, 한 파일의 두 위험이 각자 위치를 유지하는지 확인.
- 최종 설명 코드의 전체26 테스트 실행: 25통과, 기존3초 timeout 테스트1실패(457.553초). 가짜 컴파일러 시작 전 초기화가 끝나지 않아 marker가 없었음. 이전 단독 재실행은 통과해 시간 의존성 확인.
- 해당 테스트만 준비 여유를10초로 수정(가짜 컴파일러는60초 대기, 종료 상한20초); 실제 프로그램의30초/540초 기본값은 유지. 수정한 timeout + 새 다중위험 테스트2개 순차 재실행 모두통과(14.331초). 최종27개 항목은 전체 실행과 후속 실행을 합쳐 검증했으며 수정 후 전체를 다시 실행한 기록은 아님.
- Docker reporting-review 빌드·네트워크차단·2CPU/4GB·추가swap없음: 공개5+개발40+대표14+문법오류1 총60개,94.622초. 이전 ledger-integration 대비 판정60개동일. schema·stdout단일배열·이유참조/evidence대응·줄범위·중복없음·오류격리·PermissionError없음 확인.
- docs/experiments/reporting-{integration,snapshot}.json에 실제 출력·이미지ID·코드/입력/테스트 해시 보존. 기존 평가 스크립트에 이미지/출력/비교기준 옵션 추가(새 옵션 help 확인; 실제60개 실행은 동일 검사 내용의 로컬 검증 스크립트 사용).
- README와 docs/REPORTING.md, EXPANDED_RULES, 실험 안내 갱신. 원본 schema/reference와 기존 실험 결과 보존. Git push 없음.
- 한계: 근거 대응 검증은 모든 설명의 의미를 자동 증명하지 않음. 규칙이 반환한 위험들을 모으지만 금고 내부의 우선 반환 등 기존 탐지 한계 유지. 비공개 성능/이유 타당성 점수 보장 없음.
- 사용자 실습: admin_exemption 사본에 빈 줄2개를 추가해 verdict 유지/근거+2를 확인하고 관리자 우회 조건을 설명. 사용자 이해 완료는 아직 미확인.
- 다음: 제출 태그로 재빌드한 이미지를 사용하고 최종 전달 방식/의존성 라이선스 고지를 확인. 제출 목표9/20까지 달력상1일(시작 시각9/19 20:36 KST 확인).

## 9/19 sysy.json 설명 가독성 검토

- 사용자 요청으로 results/sysy.json 전체와 reporting/verdict 집계, 대회 이유 타당성 기준을 확인. 분석 코드/출력 파일은 수정하거나 재실행하지 않음.
- 파일은 공개5개뿐이며 BENIGN2/MALICIOUS3/UNCERTAIN0. 이유 수는 P1=17/P2=8/P3=14/P4=13/P5=6. 이 파일로 다른 데이터의 보류 원인은 판단할 수 없음.
- P1 안전 요약3개, P2/P5 위험 요약 각2개, P3 위험 요약3개 등 의미상 중복 확인. 완전히 같은 문자열만 제거하므로 서로 다른 규칙의 유사 결론이 남음.
- 경로별 조건/대입 사실을 reasons에 모두 붙여 판정 설명과 분석 흔적이 섞임. 위치 연결은 개선됐지만 위치 접두사도 길어져 핵심 결론을 읽기 어려움.
- 제안: 위험 유형/자산/경로 등 구조화한 분석 사실로 같은 발견을 병합하고, 서로 다른 위험은 보존. 최종 reasons는 권한→조건→자산 변경→영향 단위로 묶고 상세 경로는 별도 진단으로 분리. 임의 개수 제한이나 문구 유사도만으로 삭제하지 않음.
- UNCERTAIN 감소는 지원 분석 확대 문제이며 설명 정리만으로 해결되지 않음. 사용자 이해 완료는 미확인. 다음 구현은 사용자 요청 시 진행.

## 9/19 지원 확대 1단계 구현·검증

- 사용자 요청: 입력 Solidity 10개/전체10분을 기준으로 1단계 구현 승인. 파일별 기본30초를60초로 변경, 전체540초와 미처리 UNCERTAIN·프로세스 그룹 종료 유지. 각 파일60초를 모두 보장하는 배분은 아니며 남은 전체 예산이 우선함을 문서화.
- 공개 무상한 발행: 총량·잔액 동일 증가 및 양수 전송 가능한 장부 연결. 관리자 직접 잔액0/임의값 대입·차감: 초기 자산→일반 보유자 전송→동의 없는 변경을 확인. 자산0·전송불가·추가 미해석 조건은 보류.
- 분기형 토큰 정지: 일반 사용자 제한 경로와 관리자 우회 경로 쌍 확인. 금고: 관리 bool의 대칭 정지/OR형 관리자 예외, 장부 미차감 반복 출금, 공개 임의 ETH 송금 지원. 별도 무제한 출금 경로·0 지급·예치 없음 등 반례 보류.
- yunu 코드/판정 합산 없이 기존 Slither 선언·경로·장부 규칙을 확장. 협업자 저장소/reference/schema/패키징 의존성은 이 작업에서 수정하지 않음. claim/escrow/NFT/수수료·일반 역할 시스템은 추가하지 않음.
- tests/test_stage1.py: 독립 작성52개 입력을7개 테스트로 검증(이름 변경·Unicode 포함). tests/test_cli.py: 기본60초 및 남은 전체10초 우선 적용을 모의 시간으로 확인하는 테스트 추가.
- 중간7개 테스트 통과(156.006초). 작업 트리를 직접 마운트한 중간 전체34개 실행은33통과/설명 위치 단언1실패(563.685초). 동시에 reporting/verdict 및 관련 테스트가 변경되는 것을 확인하여 해당 변경을 보존하고 최종 소스·테스트를 별도 이미지로 고정.
- 최종 고정 이미지 stage1-tests: 전체38개 테스트 모두통과,247.216초. 네트워크차단/2CPU/4GB/추가swap없음. 공개 표본·기존 확장/장부·설명·오류 격리·시간 제한/자식 종료 포함. 검증 종료 후 소스·테스트·reference 해시가 작업 트리와 모두 동일함을 확인.
- 대표10개 실제 Docker CLI 비교: 기존 reporting-review 정상2/악성0/보류8,13.903초 → stage1-review 정상3/악성6/보류1,16.421초. 원본schema·stdout 단일배열·파일누락없음·이유/evidence 대응·줄범위 확인. 개발용 선별셋이며 비공개 성능/yunu 대비 일반 정확도로 해석하지 않음.
- 결과: docs/experiments/stage1-{integration,suite}.json, 상세 results/stage1-integration/ 및 results/stage1-suite.log. 테스트·분석 이미지 ID와 실제 내부 소스 해시를 기록. 실행 이미지와 작업 트리의 소스 차이 없음.
- Docker 기본 파일을 network none으로 새로 빌드한 시도는 설치 캐시 부재로 실패. 기존 reporting-review의 설치된 의존성에 현재 소스를 복사한 로컬 검증 이미지로 해결. 테스트 전용 빌드의 ignore 범위도 별도 지정하여 제출 .dockerignore는 유지. 최초 빌드에 인터넷이 필요한 기존 패키징 조건은 그대로이며 완성 이미지 실행만 오프라인 검증.
- README 시간 옵션, docs/DOCKER.md·EXPANDED_RULES.md, 새 docs/STAGE1.md 갱신. Git commit/push/제출 태그 교체 없음. 별도로 진행된 보고서 가독성 변경은 이번 단계의 직접 구현 기여로 기록하지 않음.
- 한계: 복잡한 자산·권한·시간·다중 출금·금고 분기형 정지 등은 계속 보류 가능. 실제 EVM 공격 거래는 실행하지 않음. 사용자가 구현을 설명/실습한 사실은 아직 미확인.
- 실습: VAULT 사본에서 장부 차감을 제거하고 두 번 출금할 때 장부와 실제 ETH가 어떻게 달라지는지 설명. 다음: 최종 제출 이미지에 소스를 반영하고 전달 방식·라이선스 고지 등 기존 제출 준비 마무리.

## 9/19 함수별 흐름 보고 구현 완료

- 사용자 요청: JSON 가독성을 높이고 권한→조건→상태 변화→사용자 영향을 실제 코드와 연결. 위험도/확률 추가 없음. 필수 네 필드 및 reasons 문자열 배열 유지.
- reporting.py의 path_claims를 함수/경로 단위로 묶고 초기 설정·권한·성공 조건·상태 변화/외부 실행을 구분. 호출 데이터와 지급액을 표시하고 writes_before로 송금/장부 변경 순서 보존.
- 규칙에서 보고용 위험 종류를 명시적으로 전달. 동일 컨트랙트의 독립 규칙 사이에서 같은 종류 및 포함 관계인 원본 쓰기/호출 위치를 병합. 변수 선언 연결을 사용하며 파일명·이름·문장 유사도로 합치지 않음. 서로 다른 위험 종류/독립 함수/같은 규칙의 다른 분기는 유지.
- verdict.py에서 컨트랙트별 보고서를 병합한 뒤 기존 MALICIOUS/UNCERTAIN/BENIGN 집계 적용. 내부 메타데이터는 stdout에 노출하지 않음. 정상은 전체 안전 확인 규칙의 대표 결론과 경로 사실을 함께 보존.
- 기존 작업 트리의 stage1 분석 확장, CLI 시간 제한 변경, 별도 작업에서 수정한 README/DOCKER/CLI 테스트를 보존. 이번 변경은 탐지 조건과 제한 시간을 변경하지 않음.
- 초기 보고 테스트1개는 위치 문자열이 단독16행이라고 가정해 실패. 묶인 줄 목록에서도 실제 상한 검사16행을 확인하도록 수정. 보고7개 통과 후 최종 전체37개 통과(381.354초). 실행 시작 뒤 추가된 기본 시간 예산 테스트1개도 별도 통과(0.022초).
- 전체 검증은 이름·Unicode·줄 이동, 분기, 상한/권한 위치, 미지원 연산, 한 컨트랙트의 독립 위험3개, 여러 컨트랙트의 위험 분리, 재진입 송금/초기화 순서, 기존 정상/위험 반례 포함.
- flow-review 이미지 빌드 후 network none·2CPU/4GB·추가swap없음: 공개5+개발40+장부14+문법오류1,60개65.284초. 이전 reporting-integration 대비 판정60개동일·schema·이유/근거 연결·중복근거없음·오류격리·PermissionError없음 확인.
- 이미지 src와 작업 트리 src SHA256 일치 확인. docs/experiments/flow-integration.json 및 flow-snapshot.json에 실제 출력/환경/코드·입력 해시 보존.
- results/sysy.json 보존, 새 실제 공개5개 출력은 results/sysy-flow.json. 이유 개수 P1 17→5/P2 8→3/P3 14→4/P4 13→5/P5 6→4. 판정은 동일.
- README, REPORTING, EXPANDED_RULES, 실험 안내 갱신. 첫 요약 뒤 초기 설정/함수 흐름을 읽으며 성공 조건이 선행 검사 순서라는 뜻은 아님을 명시.
- 남은 한계: 기존 분석 지원 범위와 금고 규칙의 우선 반환 구조 유지. 설명 정리만으로 UNCERTAIN 감소를 주장하지 않음. 독립 평가/이유 점수 보장 없음. Git push 없음.
- 사용자 실습: P3 사본 이름/줄을 바꾸고 verdict 유지와 근거 이동을 확인한 뒤, 일반 수령자가 다시 전송하지 못하는 이유를 설명. 사용자 학습 완료는 아직 확인하지 않음.
- 다음: 새 JSON 검토 후 제출 태그로 재빌드. 제출 목표9/20까지 달력상1일(작업 시작9/19 22:02 KST 확인).

## 9/19 합성300 비교 후 1단계 철회

- 동일 합성300개를 10개씩30묶음으로 reporting-review/flow-review에 순차 교대 실행. 양쪽 모두 network none·2CPU/4GB·파일60초/묶음540초. 입력/정답 해시 및 이미지ID 고정, 정답 파일은 분석기에 전달하지 않음.
- 두 버전 모두 정답40/오답5/보류255, 확정 비율15%, 확정 정확도88.89%, 순점수35. 판정 변경0개, timeout/컴파일 실패0개. 누적233.843초/236.998초. 원본 평가기와 지표 교차 검증, 600개 출력schema·근거 줄범위 확인. 기록: docs/experiments/stage1-corpus-comparison.json, results/stage1-corpus-comparison/comparison.csv.
- 사용자 요청으로 1단계 탐지 확장을 철회. expanded_tokens/expanded_vaults/ledger_operations를 이전 판정 로직으로 복원하고 보고서 병합용 메타데이터는 유지. 메타데이터를 제외한 AST가 HEAD의 세 파일과 정확히 동일함을 확인.
- tests/test_stage1.py 및 tests/evaluate_stage1_docker.py 제거. 설명 출력의 다중위험 테스트는 공개 발행 대신 기존 관리자 발행을 사용하도록 바꿔 원래의 세 독립 위험 검증 목적 유지.
- 파일별60초/전체540초 및 시간 예산 테스트, 별도 설명 출력 개선 보존. README의 현행 1단계 지원 링크 제거, STAGE1/실험 문서를 철회 전 기록으로 명시. 비교 결과와 과거 실험 증거는 보존.
- 관련 테스트9개 모두통과(23.537초): 설명7개·공개5파일 판정1개·시간예산1개. 전체 회귀/300개 재실행을 추가 수행했다고 주장하지 않음.
- 철회 상태 실행 이미지 trust404-track1:rollback-review 빌드 완료. 이전 stage1/flow 검증 이미지는 철회 전 이력이므로 현재 실행본으로 사용하지 않음. 제출 태그 교체/commit/push 없음. 다른 작업의 라이선스·의존성·gitignore 변경 보존.
- 사용자 이해/실습 완료는 미확인. 다음 구현 확대는 별도 선택 후 진행.

## 9/19 현행 코드 흐름 HTML 안내서 갱신

- 사용자 요청에 따라 기존 `docs/project-flow-guide.html`을 현행 코드 기준으로 전면 갱신. 요청에 적힌 `docx/projects-flow-guide.html` 대신 실제 존재하는 안내서 경로를 사용. 분석기 소스·탐지 규칙·테스트 코드는 이번 작업에서 변경하지 않음.
- 검사소·쿠폰·금고 비유로 전체 구조, 파일 한 장의 8단계 호출/반환 흐름, 조건·쓰기·호출 기록, P2/P4 비교, 일곱 규칙, 계약/파일 판정 집계, 보류 원인, 실행 예산과 Docker를 설명. 실제 코드 17곳을 원본에서 발췌하고 파일·행 번호·원본 SHA-256 연결.
- 파일 역할 카드20개와 검색, 단계별 보기, 계약2개 판정 실습, 남은 시간 배분 실습, Python 문법·용어 사전·읽기 순서·확인 질문 추가. 실습은 설명용이며 실제 Solidity 분석이나 성능 측정이 아님을 표시.
- 1단계 탐지 확대 철회 상태와 유지된 설명 개선·60초/540초 설정을 구분. 규칙 다수결, 단일 Unsupported의 전체 보류 오해, 부분 정상의 전체 정상 확대, Slither 기본 경고의 악성 치환을 피하도록 현재 구현에 맞춰 설명.
- 설치된 Chromium/Playwright로 로컬 file URL 확인: 원본 코드 발췌17곳 일치, 내부/파일 링크80개 유효, 판정 조합9개·시간 경계4개·파일 검색·8단계 전환·인쇄 전후 상태 복원 통과. 1440/1024/768/390/320px에서 코드 펼침 상태 포함 페이지 가로 넘침 없음. JavaScript 비활성 상태에서도 본문·파일 카드·단계 표시 확인. 브라우저 스크립트 오류0, 외부 네트워크 요청0.
- 데스크톱·모바일·실제 코드 비교 화면을 캡처해 시각 확인. 검증 중 발견한 좁은 화면의 코드 넘침과 장 번호 줄바꿈을 수정한 뒤 재확인. 로컬 검증 기록·스크린샷은 `artifacts/flow-guide-*`, 보조 생성/검증 스크립트는 ignored artifacts에 보관. 분석기 회귀 전체를 재실행한 작업은 아님. 사용자 이해 완료는 아직 미확인.

## 9/19 제출 문서·고지 정리

- 사용자 요청으로 로그를 제출 대상에서 제외하고 문서를 최신화. `*.log`, `.env.*`, 제출 이미지 tar를 ignore하며 `.env.example` 예외 유지. 이미 추적되던 `resultsseung.log`는 Git 인덱스에서만 제거했고 로컬 파일은 보존. Git 이력 삭제/commit/push는 수행하지 않음.
- AGENTS를 개인 배경·과거 일정 대신 실행 계약, 분석 정확성, 보고서·테스트, 의존성·배포 관리 중심의 개발 지침으로 재작성. README/PLAN/DOCKER/분석 설명을 현재 구현과 대조하고 철회 전 stage1·flow 실험을 현행 검증과 구분.
- THIRD_PARTY_NOTICES.md, third_party/의 설치본 원문·패키지 목록·해시, scripts/collect_notices.py 추가. Python 패키지54개 버전을 lock과 확인. crytic-compile은 설치본에 별도 고지 파일이 없어 메타데이터의 라이선스 표현과 그 한계를 명시. 대응 소스 아카이브 전체를 동봉한 자료는 아님.
- Dockerfile/.dockerignore에는 고지 복사만 추가. 판정 코드·의존성 버전·HOME·bundled solc 설정은 이번 정리에서 변경하지 않음. 기존 코드 변경 및 다른 작업에서 갱신된 HTML 설명서는 보존.
- 실제 기본 Dockerfile 빌드 성공: trust404-track1:submission-review. 이미지 ID와 소스/패키징/테스트/원본 해시는 docs/experiments/submission-check.json에 기록.
- 오프라인2CPU/4GB·추가swap없음 혼합60개: 기대 판정60/60, 38.276초. 원본 schema, 이유-위치 연결, 중복 근거 없음, 문법 오류 격리, stdout JSON, PermissionError 없음 확인. docs/experiments/submission-integration.json에 출력 보존.
- 현재 전체 회귀31개 통과, 198.769초. 초기 unittest 실행은 배포 진입점을 우회하여 개발용 기본 solc 경로가 없어 실패. 이미지 HOME의 root 소유 solc-select 하위 경로에 별칭 생성도 실패. 테스트 전용 /tmp HOME에서 bundled solc로 링크한 후 전체 재실행 통과. 실제 배포 실행은 명시적 --solc를 사용하며 변경 없음. 재현 명령은 docs/DOCKER.md에 제공.
- 이미지 src와 작업 트리 일치, 테스트 전후 src/tests/reference 및 패키징 해시 일치 확인. 설치 패키지54개와 수집 메타데이터 버전, 포함 고지 해시, 실행 HOME 쓰기 가능 확인. Markdown 링크64개·HTML 링크80개 누락 없음, HTML JavaScript 문법 및 git diff --check 통과.
- 남은 일: 최종 변경 검토·commit/push, 해당 commit으로 이미지 고정 및 주최 측 형식에 따른 전달. docs/SUBMISSION.md에 순서와 식별값 기록 방법 작성. 사용자의 설명·실습 완료나 비공개 성능은 확인한 것으로 기록하지 않음.

## 9/19 push 전 독립 실행 교차검증

- 사용자 요청으로 대회 원문/schema, README, Dockerfile, 실행 코드·판정 집계·보고서·테스트, Git 제출 범위를 다시 확인. 판정 코드 변경 없음.
- Git 추적 파일과 무시되지 않은 신규 파일147개(18,004,014bytes)만 새 디렉터리에 복사. 로그/가상환경 제외, solc·고지·실험·테스트 포함 확인. 현재 작업본 기준이며 실제 원격 clone이나 최종 commit을 검사한 것은 아님.
- 그 사본에서 기본 Dockerfile을 --no-cache·linux/amd64로 빌드 성공(온라인 준비56.558초). 새 이미지 trust404-track1:release-crosscheck; 식별값과 후보 파일 해시는 docs/experiments/release-crosscheck.json에 보존.
- 새 이미지 오프라인2CPU/4GB·추가swap없음 검증: 혼합60/60 기대 판정 일치83.554초, 전체31 테스트248.133초 통과. schema·이유/위치 연결·중복 제거·오류 격리·timeout 및 자식 종료 포함. PermissionError 없음.
- 공개5개를 case_001~005.sol로 변경하고 주석 라벨 제거·한글 주석/줄 이동 적용, 오류1개 추가. 한글·공백이 있는 호스트 디렉터리를 /cases로 연결한 별도6개 입력도 기대 판정/원본 schema/줄 범위 통과.
- 이미지 내 src와 후보 소스 해시 일치, 검증 전후 src/tests/reference/고지/패키징 변경 없음. 설치된54개 패키지 버전·고지 해시 일치, UID10001·쓰기 가능한 HOME 확인. 실행 이미지에 tests/reference/results가 없음을 확인.
- 원본 reference와 schema 변경 없음, solc SHA256 일치. 추적 로그/환경 키 파일 없음, 파일 크기100MiB 미만, 검사한 일반 비밀키 패턴 미검출. 완전한 비밀정보/라이선스 법률 감사로 주장하지 않음.
- Markdown 링크67개·HTML 링크80개 검증 당시 누락 없음; 교차검증 기록 링크를 추가 후 재확인. diff 공백 검사 통과.
- 결론: 검사한 Track1 제출 실행·출력 요건을 막는 문제는 발견하지 않음. 비공개 판정/이유 점수 보장은 아님. 현재 변경은 아직 미커밋이므로 add·commit 후 push해야 반영됨. 이 세션에서 commit/push는 하지 않음.
- 로컬 submission 태그는 현재 없음. 실제 이미지 제출 시 최종 commit으로 README의 submission 이미지를 빌드하거나 검증된 이미지 식별값을 기준으로 전달해야 함. GitHub push만으로 오프라인 이미지 반입이 완료되는 것은 아님. 최종 제출 형식은 주최 측 공통 안내를 따름.
- 사용자 이해/실습 완료는 미확인. 다음 단계는 최종 commit과 그 고정본의 제출.
